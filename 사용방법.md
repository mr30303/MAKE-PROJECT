# 프로젝트 폴더 생성기 사용방법

## 실행과 설정

`프로젝트생성기.exe`를 실행한다. Windows의 .NET Framework를 사용하며 별도 설치가 필요 없다.

- 문서 기본 위치: `D:\document`
- 소스 기본 위치: `D:\source`
- 기본값은 실행 파일 옆 `settings.json`에서 변경한다. 화면에서 바꾼 경로는 해당 실행에만 적용된다.
- 프로그램을 옮길 때는 EXE와 `settings.json`을 함께 복사한다. 설정 파일이 없으면 기본값을 사용한다. 템플릿은 필요 없다.

## 생성 순서

1. 문서 기본 위치, 프로젝트명과 시작 연도를 입력한다.
2. 소스 폴더를 함께 만들지 선택하고, 만들 경우 소스 위치와 폴더명을 입력한다.
3. 필요한 문서 하위 폴더와 보관자료를 선택한다.
4. 미리보기를 확인하고 `프로젝트 생성`을 누른 뒤 최종 확인한다.

프로젝트명에는 한글·공백·대괄호를 사용할 수 있다. 소스 폴더명은 영문·숫자·하이픈·밑줄 중심의 이름을 권장한다.

## 생성 결과

문서 폴더 이름은 `[시작연도] 프로젝트명`이다.

```text
[시작연도] 프로젝트명/
├─ 받은자료/
├─ 문서/
│  └─ 선택한 하위 폴더/
├─ 배포자료/
└─ 보관자료/             (선택)
```

문서 하위 폴더는 설계자료·개발자료·사용자매뉴얼·검수자료·감리자료·카탈로그 중 선택한다. 소스를 함께 만들면 지정한 소스 기본 위치에 별도의 빈 소스 폴더를 만든다.

**폴더만 생성하며 `AGENTS.md`, `STATUS.md`, `프로젝트정보.md` 등 Markdown 파일은 만들지 않는다.** Git 초기화·clone·커밋·push·배포도 수행하지 않는다. Git으로 clone할 예정이면 소스 생성은 선택하지 않는다.

## 기존 자료 보호

- 기존 프로젝트 폴더나 파일은 덮어쓰지 않는다. 문서·소스 경로가 같거나 서로 포함되면 중단한다.
- 고유한 임시 폴더에서 준비한 뒤 최종 위치로 이동한다. 실패하면 현재 작업이 만든 결과만 확인 후 복구한다.
- 복구 확인용 작업 표시 파일은 생성 중에만 사용하고 성공 시 제거한다. 표시 파일을 제거하지 못하면 해당 경로를 경고한다.
- 외부에서 추가·변경한 파일이 있거나 안전한 정리가 불가능하면 삭제하지 않고 남은 경로를 알린다.
- 탐색기 열기만 실패한 경우 이미 생성된 폴더는 유지한다.
- 이전 버전으로 만든 프로젝트와 기존 Markdown 파일은 변경하거나 삭제하지 않는다.

## 개발용 빌드와 테스트

`개발용\빌드.cmd`로 EXE를 다시 빌드한다. PowerShell에서는 `개발용\build.ps1 -RunTests`로 빌드와 핵심 테스트를 함께 실행한다. `-OutputPath`와 `-TestOutputPath`로 별도 출력 경로를 지정할 수 있다.
